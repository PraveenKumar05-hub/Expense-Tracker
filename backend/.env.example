PORT=5000
# For Netlify/production use a hosted MongoDB URI (Atlas/Railway/etc.), not localhost.
MONGODB_URI=mongodb+srv://<db_user>:<db_password>@<cluster-host>/<database>?retryWrites=true&w=majority
CLIENT_ORIGIN=http://localhost:3000,https://pkexpensetracker.netlify.app,https://*.netlify.app